# 命令: /python:pydantic
description = "根据描述生成一个 Pydantic 模型。"

prompt = """
你是一位精通 Python 和 Pydantic 的专家。
请根据用户的描述创建一个 Pydantic 模型。

用户的描述如下：
{{args}}

你的要求：
1.  生成的代码必须是一个完整的、可直接运行的 Pydantic 模型。
2.  所有模型字段都必须使用 `pydantic.Field` 进行详细定义，格式为 `field_name: type = Field(...)`。
3.  如果用户没有提供 `Field` 的具体参数（如 `description`, `default` 等），请根据字段名称和上下文进行合理的推断。
4.  在代码块的开头，确保包含所有必要的导入语句（例如 `from pydantic import BaseModel, Field`）。
5.  你的回答应该只包含 Python 代码块，不要有任何额外的解释。
"""
