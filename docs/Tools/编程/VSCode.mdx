---
sidebar_position: 2
---
## 自动保存

```json
{
  "files.autoSave": "afterDelay",
  "files.autoSaveDelay": 1000
}
```

## 调整界面缩放

1. 打开设置
   ```bash
   Ctrl+Shift+P (Windows/Linux)
   Cmd+Shift+P (Mac)
   ```

2. 配置缩放
   ```json
   {
     "window.zoomLevel": 1.2  // 大于1放大，小于1缩小
   }
   ```

## 安装`Fira Code`字体

1. 下载安装 [Fira Code](https://github.com/tonsky/FiraCode/releases)
   - 下载 `.zip` 文件，解压后进入 `ttf` 文件夹
   - 全选所有 `.ttf` 文件 → 右键 → **"安装"**

2. 配置 VS Code
   ```json
   {
     "editor.fontFamily": "Fira Code",
     "editor.fontLigatures": true, // 启用连字
     "editor.fontSize": 16  // 可选
   }
   ```

## powershell7作为默认终端

```json
"terminal.integrated.profiles.windows": {
    "PowerShell": {
        "source": "PowerShell",
        "icon": "terminal-powershell",
        "path": "D:\\Applications\\PowerShell7\\pwsh.exe"
    }
}
```

## 无法连接到`wsl`主机

1. 完全退出 VSCode。
2. 在 WSL2 终端中，输入 `rm -rf ~/.vscode-server/` 并回车。
3. 再次打开 VSCode。


参考[这里](https://github.com/microsoft/vscode/issues/187360#issuecomment-1722550316)

## 用`chrome`调试react

```json
// 在launch.json中添加
{
    "name": "Run Chrome",
    "type": "chrome",
    "request": "launch",
    "url": "http://localhost:1420",
    "webRoot": "${workspaceFolder}"
}
```
