## 删除本地所有非`main`分支

```powershell
git branch | Where-Object { $_ -notmatch "main" -and $_ -notmatch "^\*" } | ForEach-Object { git branch -D $_.Trim() }
```


## 不用每次输密码

```bash

git config --global credential.helper store
```


## 将某个文件重置成仓库版本

```bash
git checkout -- <file>
```