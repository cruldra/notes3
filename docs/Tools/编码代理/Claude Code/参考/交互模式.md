---
sidebar_position: 5
---

# 交互模式

> Claude Code 会话中键盘快捷键、输入模式和交互功能的完整参考。

## 键盘快捷键

> **注意**：键盘快捷键可能因平台和终端而异。按 `?` 查看你环境中可用的快捷键。
>
> **macOS 用户**：Option/Alt 键快捷键（`Alt+B`、`Alt+F`、`Alt+Y`、`Alt+M`、`Alt+P`、`Alt+T`）需要在终端中将 Option 配置为 Meta：
>
> - **iTerm2**：设置 → Profiles → Keys → 将左/右 Option 键设置为"Esc+"
> - **Terminal.app**：设置 → Profiles → Keyboard → 勾选"Use Option as Meta Key"
> - **VS Code**：在 VS Code 设置中设置 `"terminal.integrated.macOptionIsMeta": true`
>
> 详情见[终端配置](/en/terminal-config)。

### 通用控制

| 快捷键                                            | 描述                                             | 上下文                                                                                                                                                                                                                                                                                       |
| :------------------------------------------------ | :----------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Ctrl+C`                                          | 取消当前输入或生成                                | 标准中断                                                                                                                                                                                                                                                                                     |
| `Ctrl+X Ctrl+K`                                   | 终止所有后台代理。3 秒内按两次确认                | 后台代理控制                                                                                                                                                                                                                                                                                 |
| `Ctrl+D`                                          | 退出 Claude Code 会话                             | EOF 信号                                                                                                                                                                                                                                                                                     |
| `Ctrl+G` 或 `Ctrl+X Ctrl+E`                       | 在默认文本编辑器中打开                           | 在默认文本编辑器中编辑你的提示或自定义响应。`Ctrl+X Ctrl+E` 是 readline 原生绑定                                                                                                                                                                                                             |
| `Ctrl+L`                                          | 清除提示输入                                     | 清除键入的文本，保留对话历史                                                                                                                                                                                                                                                                 |
| `Ctrl+O`                                          | 切换转录查看器                                   | 显示详细的工具使用和执行情况。还会展开 MCP 读取和搜索调用，这些调用默认折叠为单行如"Queried slack"。在[全屏渲染](/en/fullscreen)中，循环三种状态：正常提示、转录模式和焦点视图（最后提示 + 工具摘要 + 响应）                                                                                |
| `Ctrl+R`                                          | 反向搜索命令历史                                 | 交互式搜索之前的命令                                                                                                                                                                                                                                                                         |
| `Ctrl+V` 或 `Cmd+V`（iTerm2）或 `Alt+V`（Windows） | 从剪贴板粘贴图片                                   | 在光标处插入 `[Image #N]` 芯片，这样你可以在提示中按位置引用它                                                                                                                                                                                                                               |
| `Ctrl+B`                                          | 后台运行任务                                     | 将 bash 命令和代理放到后台。tmux 用户按两次                                                                                                                                                                                                                                                  |
| `Ctrl+T`                                          | 切换任务列表                                     | 在终端状态区域显示或隐藏[任务列表](#任务列表)                                                                                                                                                                                                                                                |
| `左/右箭头`                                        | 循环切换对话框选项卡                              | 在权限对话框和菜单中的选项卡之间导航                                                                                                                                                                                                                                                         |
| `上/下箭头`                                        | 导航命令历史                                     | 回忆之前的输入                                                                                                                                                                                                                                                                               |
| `Esc` + `Esc`                                     | 回滚或总结                                       | 将代码和/或对话恢复到之前的点，或从选定的消息开始总结                                                                                                                                                                                                                                        |
| `Shift+Tab` 或 `Alt+M`（某些配置）                 | 循环切换权限模式                                 | 循环切换 `default`、`acceptEdits`、`plan` 和你启用的任何模式，如 `auto` 或 `bypassPermissions`。见[权限模式](/en/permission-modes)                                                                                                                                                            |
| `Option+P`（macOS）或 `Alt+P`（Windows/Linux）    | 切换模型                                         | 切换模型而不清除你的提示                                                                                                                                                                                                                                                                     |
| `Option+T`（macOS）或 `Alt+T`（Windows/Linux）    | 切换扩展思考                                     | 启用或禁用扩展思考模式。在 macOS 上，配置你的终端将 Option 发送为 Meta 以此快捷键生效                                                                                                                                                                                                        |
| `Option+O`（macOS）或 `Alt+O`（Windows/Linux）    | 切换快速模式                                     | 启用或禁用[快速模式](/en/fast-mode)                                                                                                                                                                                                                                                          |

### 文本编辑

| 快捷键                   | 描述               | 上下文                                                                                             |
| :----------------------- | :----------------- | :------------------------------------------------------------------------------------------------- |
| `Ctrl+K`                 | 删除到行尾         | 存储删除的文本用于粘贴                                                                             |
| `Ctrl+U`                 | 从光标删除到行首   | 存储删除的文本用于粘贴。在多行输入中重复以跨行清除                                                   |
| `Ctrl+Y`                 | 粘贴删除的文本     | 粘贴用 `Ctrl+K` 或 `Ctrl+U` 删除的文本                                                             |
| `Alt+Y`（在 `Ctrl+Y` 后） | 循环粘贴历史       | 粘贴后，循环遍历之前删除的文本。macOS 上需要[Option as Meta](#键盘快捷键)                            |
| `Alt+B`                  | 光标向后移动一个词 | 词导航。macOS 上需要[Option as Meta](#键盘快捷键)                                                    |
| `Alt+F`                  | 光标向前移动一个词 | 词导航。macOS 上需要[Option as Meta](#键盘快捷键)                                                    |

### 主题和显示

| 快捷键   | 描述                     | 上下文                                                                        |
| :------- | :----------------------- | :---------------------------------------------------------------------------- |
| `Ctrl+T` | 切换代码块的语法高亮     | 仅在 `/theme` 选择器菜单内有效。控制 Claude 响应中的代码是否使用语法着色        |

### 多行输入

| 方法         | 快捷键         | 上下文                                                |
| :----------- | :------------- | :---------------------------------------------------- |
| 快速转义     | `\` + `Enter`  | 适用于所有终端                                        |
| macOS 默认   | `Option+Enter` | macOS 上的默认                                        |
| Shift+Enter  | `Shift+Enter`  | 在 iTerm2、WezTerm、Ghostty、Kitty 中开箱即用         |
| 控制序列     | `Ctrl+J`       | 用于多行输入的换行符                                  |
| 粘贴模式     | 直接粘贴       | 用于代码块、日志                                      |

> **提示**：Shift+Enter 在 iTerm2、WezTerm、Ghostty 和 Kitty 中无需配置即可工作。对于其他终端（VS Code、Alacritty、Zed、Warp），运行 `/terminal-setup` 安装绑定。

### 快速命令

| 快捷键          | 描述           | 说明                                                       |
| :-------------- | :------------- | :--------------------------------------------------------- |
| 开头的 `/`      | 命令或技能     | 见[命令](#命令)和[技能](/en/skills)                        |
| 开头的 `!`      | Bash 模式      | 直接运行命令并将执行输出添加到会话中                       |
| `@`             | 文件路径提及   | 触发文件路径自动补全                                       |

### 转录查看器

打开转录查看器时（用 `Ctrl+O` 切换），这些快捷键可用。`Ctrl+E` 可以通过 [`transcript:toggleShowAll`](/en/keybindings) 重新绑定。

| 快捷键               | 描述                                                         |
| :------------------- | :----------------------------------------------------------- |
| `Ctrl+E`             | 切换显示所有内容                                             |
| `q`、`Ctrl+C`、`Esc` | 退出转录视图。三者都可以通过 [`transcript:exit`](/en/keybindings) 重新绑定 |

### 语音输入

| 快捷键       | 描述               | 说明                                                                                                                                                     |
| :----------- | :----------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 按住 `Space` | 按键说话听写       | 需要启用[语音听写](/en/voice-dictation)。转录插入在光标处。[可重新绑定](/en/voice-dictation#rebind-the-push-to-talk-key)                                     |

---

## 命令

在 Claude Code 中输入 `/` 查看所有可用命令，或输入 `/` 后跟任意字母进行过滤。`/` 菜单显示你可以调用的所有内容：内置命令、捆绑和用户编写的[技能](/en/skills)，以及[插件](/en/plugins)和 [MCP 服务器](/en/mcp#use-mcp-prompts-as-commands)贡献的命令。并非所有内置命令对每个用户都可见，因为某些命令取决于你的平台或计划。

见[命令参考](/en/commands)获取 Claude Code 中包含的命令完整列表。

---

## Vim 编辑模式

通过 `/config` → Editor mode 启用 vim 风格编辑。

### 模式切换

| 命令    | 动作               | 来源模式 |
| :------ | :----------------- | :------- |
| `Esc`   | 进入 NORMAL 模式   | INSERT   |
| `i`     | 在光标前插入       | NORMAL   |
| `I`     | 在行首插入         | NORMAL   |
| `a`     | 在光标后插入       | NORMAL   |
| `A`     | 在行尾插入         | NORMAL   |
| `o`     | 在下方打开新行     | NORMAL   |
| `O`     | 在上方打开新行     | NORMAL   |

### 导航（NORMAL 模式）

| 命令            | 动作                                           |
| :-------------- | :--------------------------------------------- |
| `h`/`j`/`k`/`l` | 左/下/上/右移动                                |
| `w`             | 下一个词                                       |
| `e`             | 词尾                                           |
| `b`             | 上一个词                                       |
| `0`             | 行首                                           |
| `$`             | 行尾                                           |
| `^`             | 第一个非空白字符                               |
| `gg`            | 输入开头                                       |
| `G`             | 输入结尾                                       |
| `f{char}`       | 跳转到字符的下一个出现位置                     |
| `F{char}`       | 跳转到字符的上一个出现位置                     |
| `t{char}`       | 跳转到下一个出现位置之前                     |
| `T{char}`       | 跳转到上一个出现位置之后                     |
| `;`             | 重复上次 f/F/t/T 动作                          |
| `,`             | 反向重复上次 f/F/t/T 动作                      |

> **注意**：在 vim 普通模式中，如果光标在输入的开头或结尾且无法进一步移动，`j`/`k` 和方向键会改为导航命令历史。

### 编辑（NORMAL 模式）

| 命令           | 动作                   |
| :------------- | :--------------------- |
| `x`            | 删除字符               |
| `dd`           | 删除行                 |
| `D`            | 删除到行尾             |
| `dw`/`de`/`db` | 删除词/到词尾/到词首   |
| `cc`           | 更改行                 |
| `C`            | 更改到行尾             |
| `cw`/`ce`/`cb` | 更改词/到词尾/到词首   |
| `yy`/`Y`       | 复制行                 |
| `yw`/`ye`/`yb` | 复制词/到词尾/到词首   |
| `p`            | 在光标后粘贴           |
| `P`            | 在光标前粘贴           |
| `>>`           | 缩进行                 |
| `<<`           | 减少缩进               |
| `J`            | 连接行                 |
| `.`            | 重复上次更改           |

### 文本对象（NORMAL 模式）

文本对象与 `d`、`c` 和 `y` 等操作符一起使用：

| 命令      | 动作                                 |
| :-------- | :----------------------------------- |
| `iw`/`aw` | 词内/词周围                          |
| `iW`/`aW` | 大词内/大词周围（空白分隔）          |
| `i"`/`a"` | 双引号内/双引号周围                  |
| `i'`/`a'` | 单引号内/单引号周围                  |
| `i(`/`a(` | 括号内/括号周围                      |
| `i[`/`a[` | 方括号内/方括号周围                  |
| `i{`/`a{` | 花括号内/花括号周围                  |

---

## 命令历史

Claude Code 维护当前会话的命令历史：

- 输入历史按工作目录存储
- 输入历史在运行 `/clear` 开始新会话时重置。之前会话的对话被保留且可以恢复。
- 使用上/下箭头导航（见上面的键盘快捷键）
- **注意**：历史扩展（`!`）默认禁用

### 用 Ctrl+R 反向搜索

按 `Ctrl+R` 交互式搜索你的命令历史：

1. **开始搜索**：按 `Ctrl+R` 激活反向历史搜索
2. **输入查询**：输入要在之前命令中搜索的文本。搜索词在匹配结果中高亮显示
3. **导航匹配项**：再次按 `Ctrl+R` 循环遍历更旧的匹配项
4. **接受匹配项**：
   - 按 `Tab` 或 `Esc` 接受当前匹配项并继续编辑
   - 按 `Enter` 接受并立即执行命令
5. **取消搜索**：
   - 按 `Ctrl+C` 取消并恢复原始输入
   - 在空搜索上按 `Backspace` 取消

搜索显示匹配的命令，搜索词高亮显示，因此你可以找到并复用之前的输入。

---

## 后台 bash 命令

Claude Code 支持在后台运行 bash 命令，允许你在长时间运行的进程执行时继续工作。

### 后台工作原理

当 Claude Code 在后台运行命令时，它异步运行命令并立即返回后台任务 ID。Claude Code 可以在命令继续在后台执行时响应新提示。

要在后台运行命令，你可以：

- 提示 Claude Code 在后台运行命令
- 按 Ctrl+B 将常规 Bash 工具调用移到后台。（由于 tmux 的前缀键，tmux 用户必须按两次 Ctrl+B。）

**关键功能：**

- 输出写入文件，Claude 可以使用 Read 工具检索它
- 后台任务有唯一 ID 用于跟踪和输出检索
- 后台任务在 Claude Code 退出时自动清理
- 当输出超过 5GB 时，后台任务自动终止，stderr 中有注释解释原因

要禁用所有后台任务功能，将 `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS` 环境变量设置为 `1`。详情见[环境变量](/en/env-vars)。

**常见的后台命令：**

- 构建工具（webpack、vite、make）
- 包管理器（npm、yarn、pnpm）
- 测试运行器（jest、pytest）
- 开发服务器
- 长时间运行的进程（docker、terraform）

### 带 `!` 前缀的 Bash 模式

通过在输入前加 `!` 直接运行 bash 命令而无需经过 Claude：

```bash
! npm test
! git status
! ls -la
```

Bash 模式：

- 将命令及其输出添加到对话上下文中
- 显示实时进度和输出
- 支持长时间运行命令的相同 `Ctrl+B` 后台化
- 不需要 Claude 解释或批准命令
- 支持基于历史的自动补全：输入部分命令并按 **Tab** 从当前项目中之前的 `!` 命令完成
- 在空提示上用 `Escape`、`Backspace` 或 `Ctrl+U` 退出
- 将空格开头的文本粘贴到空提示中时自动进入 bash 模式，与键入 `!` 行为匹配

这对于快速 shell 操作同时保持对话上下文很有用。

---

## 提示建议

当你首次打开会话时，提示输入中会出现灰色示例命令来帮助你开始。Claude Code 从项目的 git 历史中选择，因此它反映你最近正在工作的文件。

Claude 响应后，建议会继续基于你的对话历史出现，如多部分请求的后续步骤或工作流的自然延续。

- 按 **Tab** 或 **右箭头** 接受建议，或按 **Enter** 接受并提交
- 开始打字以关闭它

建议作为后台请求运行，复用父对话的提示缓存，因此额外成本最小。当缓存冷时，Claude Code 跳过建议生成以避免不必要的成本。

建议在对话的第一轮之后、非交互模式和计划模式中自动跳过。

要完全禁用提示建议，设置环境变量或在 `/config` 中切换设置：

```bash
export CLAUDE_CODE_ENABLE_PROMPT_SUGGESTION=false
```

---

## 用 /btw 边问

使用 `/btw` 询问关于你当前工作的快速问题而不添加到对话历史中。这在你想要快速答案但不想弄乱主上下文或使 Claude 偏离长时间运行的任务时很有用。

```
/btw what was the name of that config file again?
```

边问对当前对话完全可见，因此你可以询问 Claude 已经读取的代码、它之前做出的决策或会话中的任何其他内容。问题和答案是短暂的：它们出现在可关闭的覆盖层中，从不进入对话历史。

- **在 Claude 工作时可用**：即使在 Claude 处理响应时你也可以运行 `/btw`。边问独立运行，不会中断主轮次。
- **无工具访问**：边问仅从已在上下文中的内容回答。Claude 在回答边问时无法读取文件、运行命令或搜索。
- **单次响应**：没有后续轮次。如果你需要来回交互，改用正常提示。
- **低成本**：边问复用父对话的提示缓存，因此额外成本最小。

按 **Space**、**Enter** 或 **Escape** 关闭答案并返回提示。

`/btw` 是[子代理](/en/sub-agents)的反面：它看到你的完整对话但没有工具，而子代理有完整工具但从空上下文开始。用 `/btw` 询问 Claude 已经从此会话中知道的内容；用子代理去查找新内容。

---

## 任务列表

在处理复杂的多步骤工作时，Claude 创建任务列表来跟踪进度。任务出现在终端的状态区域，带有指示器显示什么待处理、进行中或完成。

- 按 `Ctrl+T` 切换任务列表视图。显示最多 10 个任务
- 要查看所有任务或清除它们，直接询问 Claude："show me all tasks"或"clear all tasks"
- 任务跨上下文压缩持久化，帮助 Claude 在较大项目上保持组织
- 要跨会话共享任务列表，设置 `CLAUDE_CODE_TASK_LIST_ID` 使用 `~/.claude/tasks/` 中的命名目录：`CLAUDE_CODE_TASK_LIST_ID=my-project claude`

---

## PR 审查状态

当在带有开放拉取请求的分支上工作时，Claude Code 在底部显示可点击的 PR 链接（例如"PR #446"）。链接有带颜色的下划线指示审查状态：

- 绿色：已批准
- 黄色：待审查
- 红色：请求更改
- 灰色：草稿
- 紫色：已合并

`Cmd+点击`（Mac）或 `Ctrl+点击`（Windows/Linux）链接在浏览器中打开拉取请求。状态每 60 秒自动更新。

> **注意**：PR 状态需要安装并认证 `gh` CLI（`gh auth login`）。

---

## 另见

- [技能](/en/skills) — 自定义提示和工作流
- [检查点](/en/checkpointing) — 回滚 Claude 的编辑并恢复之前的状态
- [CLI 参考](/en/cli-reference) — 命令行标志和选项
- [设置](/en/settings) — 配置选项
- [记忆管理](/en/memory) — 管理 CLAUDE.md 文件

---

## 提交反馈

如果你发现本文档中有不正确、过时或令人困惑的内容，请通过 POST 提交反馈到：
https://code.claude.com/docs/_mintlify/feedback/claude-code/agent-feedback

请求体（JSON）：`{ "path": "/current-page-path", "feedback": "问题描述" }`

仅在你有具体且可操作的报告时提交反馈。
