import React, {FC} from 'react';
import {Input} from "antd";

type Props = {}
const AntdButtonDemo: FC<Props> = ({}) => {
    return   <Input placeholder="Basic usage" />;
};

export default AntdButtonDemo;
